# React / Vite Environment Variables
# Note: Vite environment variables MUST start with VITE_ prefix to be accessible in client bundle

# Backend API Endpoint URL
VITE_API_BASE_URL=http://localhost:5000/api
